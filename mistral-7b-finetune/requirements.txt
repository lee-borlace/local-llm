# Python 3.10 or 3.11 recommended (best compatibility)

# Core ML libraries
# NOTE: Install PyTorch with CUDA first using:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# Then install the rest: pip install -r requirements.txt
torch>=2.5.0,<3.0.0  # PyTorch with CUDA 12.1+ support (install separately!)
transformers>=4.57.0  # Latest with Mistral support
datasets>=3.0.0  # For loading training data
accelerate>=1.2.0  # Multi-GPU & distributed training

# Fine-tuning libraries (latest versions as of Dec 2025)
trl>=0.26.0  # Transformer Reinforcement Learning (SFTTrainer)
peft>=0.13.0  # Parameter-Efficient Fine-Tuning (LoRA/QLoRA)
bitsandbytes>=0.44.0  # 4-bit quantization

# Optional but recommended
sentencepiece>=0.2.0  # Tokenizer for some models
protobuf>=3.20.0  # For tokenizers
scipy>=1.10.0  # Scientific computing
